{% extends '::base.html.twig' %}

{% block body %}
    <h1>XSSAttack attack</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
    {{ form_end(form) }}

    <ul>
        <li>
            <a href="{{ path('xssattack_index') }}">Back to the list</a>
        </li>
    <ul>

{% endblock %}
